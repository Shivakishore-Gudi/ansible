- name: install packages
  host: local
  connection: local
  become: yes
  tasks:
    - name: print names
      ansible.builtin.packages:
        name: "{{ item }}"
        state: latest
      loop:
      - nginx
      - mysql
      - zip
